# ========================================
# eCertificate Environment Configuration
# ========================================
# Copy this file to .env and update with your actual values
# Windows users: Run .\scripts\windows\setup.ps1 to auto-create .env

# ========================================
# Flask Configuration
# ========================================
FLASK_APP=app.py
FLASK_ENV=development
FLASK_DEBUG=True
SECRET_KEY=your-secret-key-here-change-in-production
PORT=5000

# ========================================
# Database Configuration
# ========================================
# Database Configuration
# ========================================
# The application now uses SQLite by default (file-based database, no server needed).
# See DATABASE_OPTIONS.md for alternatives (PostgreSQL, MySQL, MongoDB, etc.)

# SQLite Configuration (DEFAULT - no setup required)
# ---------------------------------
# File-based database (recommended for development and small deployments)
DATABASE_URL=sqlite:///ecertificate.db

# Alternative Database Options (see DATABASE_OPTIONS.md for setup instructions)
# --------------------------------------------------------------------------------
# PostgreSQL:
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/ecertificate

# MySQL/MariaDB:
# DATABASE_URL=mysql+pymysql://root:rootpassword@mysql:3306/ecertificate

# MongoDB (requires reverting to MongoDB code - see git history):
# MONGO_URI=mongodb://mongo:27017/eCertificate
# MONGO_URI=mongodb://host.docker.internal:27017/eCertificate  # For host machine
# MONGO_URI="mongodb+srv://<user>:<password>@<cluster>.mongodb.net/eCertificate"  # MongoDB Atlas

# Redis (for hybrid setup with PostgreSQL):
# REDIS_URL=redis://redis:6379/0


# ========================================
# Email Configuration (SMTP)
# ========================================
# Gmail configuration (recommended for Windows local dev)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-gmail-app-password
MAIL_DEFAULT_SENDER=your-email@gmail.com

# Outlook/Office365 configuration:
# MAIL_SERVER=smtp.office365.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your-email@outlook.com
# MAIL_PASSWORD=your-password

# Other SMTP providers:
# MAIL_SERVER=smtp.example.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your-email@example.com
# MAIL_PASSWORD=your-password

# ========================================
# Telegram Bot Configuration
# ========================================
# Get your bot token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here
# Optional: Webhook URL for Telegram (use ngrok for local dev on Windows)
# TELEGRAM_WEBHOOK_URL=https://your-ngrok-url.ngrok.io

# ========================================
# Upload Configuration
# ========================================
UPLOAD_FOLDER=uploads
MAX_CONTENT_LENGTH=16777216
ALLOWED_EXTENSIONS=png,jpg,jpeg,svg

# ========================================
# Certificate Generation
# ========================================
OUTPUT_FOLDER=generated_certificates

# Alignment Verification Settings
# Ensures generated certificates match reference sample with <0.01px difference
ENABLE_ALIGNMENT_CHECK=True           # Enable pixel-perfect alignment verification
ALIGNMENT_TOLERANCE_PX=0.01           # Maximum allowed pixel difference (default: 0.01)
ALIGNMENT_MAX_ATTEMPTS=3              # Number of verification attempts before failure

# Field Position Verification Settings
# Verifies that text field Y-coordinates match the reference sample_certificate.png
FIELD_POSITION_TOLERANCE_PX=2         # Maximum allowed Y-coordinate offset in pixels (default: 2)

# ========================================
# Azure Configuration (Optional)
# ========================================
# Uncomment and fill these if you're using Azure services
# AZURE_STORAGE_CONNECTION_STRING=
# AZURE_STORAGE_CONTAINER_NAME=
# AZURE_BLOB_ACCOUNT_NAME=
# AZURE_BLOB_ACCOUNT_KEY=

# ========================================
# Additional Configuration (Optional)
# ========================================
# LOG_LEVEL=INFO
# CERTIFICATE_FONT_PATH=fonts/Arial.ttf
# CERTIFICATE_FONT_SIZE=48
